import 'package:flutter/material.dart';

class GalleryHead extends StatelessWidget {
  const GalleryHead({super.key});

  

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}